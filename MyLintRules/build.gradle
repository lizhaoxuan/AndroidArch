apply plugin: 'java'

dependencies {
    compile 'com.android.tools.lint:lint-api:24.2.3'
    compile 'com.android.tools.lint:lint-checks:24.2.3'
}

jar {
    manifest {
        attributes("Lint-Registry": "me.ycdev.android.arch.lint.MyIssueRegistry")
    }
}

defaultTasks 'assemble'

/*
 * rules for providing "MyLintRules.jar"
 */
configurations {
    lintJarOutput
}

dependencies {
    lintJarOutput files(jar)
}
